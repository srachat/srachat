version: '3'

services:
  web:
    build: .
    command: bash -c "python backend/manage.py migrate && python backend/manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/srachat
    ports:
      - "8000:8000"